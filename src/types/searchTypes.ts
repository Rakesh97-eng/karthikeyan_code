export interface SearchTypes {
  brand: string;
  productName: string;
  price: string;
  chip: string;
  dateOfBought: string;
  productImage: string;
  cardPayment: boolean;
  isSelected: boolean;
}
